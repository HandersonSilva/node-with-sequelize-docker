{
  "name": "sqlnode",
  "version": "0.0.1",
  "main": "src/server.js",
  "scripts": {
    "start": "nodemon src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1",
    "mariadb": "^2.5.5",
    "mysql2": "^2.3.3",
    "nodemon": "^2.0.15",
    "sequelize": "^6.12.0-beta.2"
  },
  "devDependencies": {
    "sequelize-cli": "^6.3.0"
  }
}
